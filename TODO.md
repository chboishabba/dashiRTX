# TODO

- Add q99‑targeted refinement loop with a quality threshold and max‑leaves guard.
- Prevent quadtree leaf collapse with adaptive refine thresholds.
- Add multi‑frame loss support for TV/metric terms in training.
- Connect error‑map refinement to the training loop (render‑measure‑refine‑retrain).
- Expand operator‑learning stub into a multi‑frame, view‑conditioned K model.
- Add benchmarks for higher bounce costs (e.g., 50–100) and extreme sparsity regimes.
